const RDS = artifacts.require('RDS')

module.exports = function (deployer) {
  // deployer.then(async () => {
  // 	await deployer.deploy(RDS)
  // 	console.log('RDS address:', RDS.address)
  // })
}
